# Contributing

欢迎提交修复和新主题。请保持 ESM、Node.js 20+ 兼容，避免把 WorkBuddy DOM selector 散落到业务代码，优先通过 adapter 和诊断层扩展。

提交前运行 `npm run lint` 和 `npm test`。新增导入器、CDP、注入器或守护行为时，请同时添加 mock 测试。主题素材必须附许可证和来源说明，不得提交未经授权的商业 IP。
